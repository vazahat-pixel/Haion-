# Production defaults (committed). Override in Vercel dashboard if needed.
# API calls use same-origin /api — proxied to BACKEND_URL via middleware.js
VITE_API_BASE_URL=/api
VITE_APP_NAME=Haion ERP
VITE_APP_VERSION=1.0.0
VITE_USE_MOCK_API=false
